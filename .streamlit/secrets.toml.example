# Copy to .streamlit/secrets.toml and fill in.
# On Streamlit Community Cloud, paste the same contents into
# App settings -> Secrets (do NOT commit the real file).

ANTHROPIC_API_KEY = "sk-ant-..."
# OPENAI_API_KEY = "sk-..."
